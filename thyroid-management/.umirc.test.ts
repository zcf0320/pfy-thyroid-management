import { defineConfig } from 'umi';
export default defineConfig({
  define: {
    QUESTIONNAIRE_URL: 'https://questionnaire-fe-test.g-hcare.com/',
    MINAPP_URL:
      'https://service-test.g-hcare.com/questionnaire/#/questionnaire/custom/start?id=155&channelId=CH000008',
  },
});
